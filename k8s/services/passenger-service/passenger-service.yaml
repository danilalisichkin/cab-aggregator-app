apiVersion: apps/v1
kind: Deployment
metadata:
  name: passenger-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: passenger-service
  template:
    metadata:
      labels:
        app: passenger-service
    spec:
      containers:
        - name: cab-passenger-service
          image: cab-passenger-service:1.0.0
          envFrom:
            - configMapRef:
                name: passenger-service-config
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - wget --spider --quiet http://localhost:8082/actuator/health && wget -qO- http://localhost:8082/actuator/health | grep UP || exit 1
            failureThreshold: 5
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
          ports:
            - containerPort: 8082
              protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: passenger-service
spec:
  type: ClusterIP
  selector:
    app: passenger-service
  ports:
    - port: 8082
      targetPort: 8082
