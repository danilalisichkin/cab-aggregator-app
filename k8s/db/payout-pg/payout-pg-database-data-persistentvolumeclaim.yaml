apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: payout-pg-database-data
  name: payout-pg-database-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
