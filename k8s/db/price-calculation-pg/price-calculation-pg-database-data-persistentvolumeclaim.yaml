apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: price-calculation-pg-database-data
  name: price-calculation-pg-database-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
