apiVersion: apps/v1
kind: Deployment
metadata:
  name: price-calculation-pg-database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: price-calculation-pg-database
  template:
    metadata:
      labels:
        app: price-calculation-pg-database
    spec:
      containers:
        - name: cab-price-calculation-pg-database
          image: postgres:17
          envFrom:
            - configMapRef:
                name: price-calculation-pg-database-config
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U $POSTGRES_USER -d $POSTGRES_DB
            failureThreshold: 5
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: price-calculation-pg-database-data
      volumes:
        - name: price-calculation-pg-database-data
          persistentVolumeClaim:
            claimName: price-calculation-pg-database-data
---
apiVersion: v1
kind: Service
metadata:
  name: price-calculation-pg-database
spec:
  type: ClusterIP
  selector:
    app: price-calculation-pg-database
  ports:
    - port: 5432
      targetPort: 5432
