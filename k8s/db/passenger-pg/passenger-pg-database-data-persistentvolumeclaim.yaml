apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: passenger-pg-database-data
  name: passenger-pg-database-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
