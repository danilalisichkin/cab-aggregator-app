apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: driver-pg-database-data
  name: driver-pg-database-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
