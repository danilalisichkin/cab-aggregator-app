apiVersion: apps/v1
kind: Deployment
metadata:
  name: driver-pg-database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: driver-pg-database
  template:
    metadata:
      labels:
        app: driver-pg-database
    spec:
      containers:
        - name: cab-driver-pg-database
          image: postgres:17
          envFrom:
            - configMapRef:
                name: driver-pg-database-config
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pg_isready -U $POSTGRES_USER -d $POSTGRES_DB
            failureThreshold: 5
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
          ports:
            - containerPort: 5432
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: driver-pg-database-data
      volumes:
        - name: driver-pg-database-data
          persistentVolumeClaim:
            claimName: driver-pg-database-data
---
apiVersion: v1
kind: Service
metadata:
  name: driver-pg-database
spec:
  type: ClusterIP
  selector:
    app: driver-pg-database
  ports:
    - port: 5432
      targetPort: 5432
